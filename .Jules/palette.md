## $(date +%Y-%m-%d) - Surface hidden terminal shortcuts
**Learning:** Standard terminal shortcuts like `Ctrl+u` and `Ctrl+k` for text deletion are highly useful but often undiscoverable to non-power users.
**Action:** Always add explicit keyboard shortcut hints to help menus or footers for "hidden" power user features to improve accessibility and UX.
