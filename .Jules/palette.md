## 2026-03-28 - Accessible Redirects
**Learning:** Adding `aria-live` to automatic redirect countdowns and dynamically shown authentication dialogs is critical for screen reader users to understand background transitions.
**Action:** Always verify keyboard/screen reader experience on automated UI flows, adding `aria-live='assertive'` or `'polite'` where appropriate.
